# Template only (no real credentials here).
# Keep real credentials only in the project root `.env`.

# Auth0 (required)
AUTH0_SECRET=your-auth0-secret-min-32-characters-long
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_CLIENT_ID=your-auth0-client-id
AUTH0_CLIENT_SECRET=your-auth0-client-secret
APP_BASE_URL=http://localhost:3000

# Auth0 API Access Token contract (required for backend calls)
AUTH0_API_AUDIENCE=https://your-api-identifier
AUTH0_API_SCOPES=read:app
AUTH0_LOGIN_SCOPES=openid profile email

# Public mirrors used by browser token helper
NEXT_PUBLIC_AUTH0_API_AUDIENCE=https://your-api-identifier
NEXT_PUBLIC_AUTH0_API_SCOPES=read:app

# Backend API URLs
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
API_URL=http://backend:8000

# Backend runtime policy (set in root .env)
# AUDIT_LOCAL_ARTIFACTS_ENABLED=false
# LEGACY_API_REDIRECT_ENABLED=false
